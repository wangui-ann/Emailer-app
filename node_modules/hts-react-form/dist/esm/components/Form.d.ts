/// <reference types="react" />
import { FormProps } from '../types';
declare function Form({ controls, actionControl, forgetPassword, googleLogin, githubLogin, facebookLogin, labelClassName, labelStyles, styles, className, errorClassName, errorStyles, }: FormProps): JSX.Element;
export default Form;
